<div class="card mb-5">
    <div class="card-body">
        <p>{{this.origin}}</p>
        <p>{{this.shortUrl}}</p>
        <a href="/eliminar/{{this._id}}" class="btn btn-danger">Eliminar</a>
        <a href="/editar/{{this._id}}" class="btn btn-warning">Editar</a>
        <a data-short="{{this.shortUrl}}" href="#" class="btn btn-info">Copiar</a>
    </div>
</div>